import jee6;

extension de::itemis::jee6::generator::ext::types;

String finderOf(OptionRef reference) :
	"find" + typeOf(reference).toFirstUpper();

String listOf(OptionRef reference) :
	"getActive" + typeOf(reference).toFirstUpper() + "List";
